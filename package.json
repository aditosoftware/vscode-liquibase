{
  "name": "liquibase",
  "displayName": "Liquibase",
  "description": "execute liquibase changelogs",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.84.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "liquibase.createLiquibaseConfiguration",
        "title": "Create Liquibase Configuration",
        "category": "Liquibase"
      },
      {
        "command": "liquibase.addExistingConfiguration",
        "title": "Add existing liquibase.properties to the configuration",
        "category": "Liquibase"
      },
      {
        "command": "liquibase.testConfiguration",
        "title": "Tests an existing liquibase configuration",
        "category": "Liquibase"
      }
    ],
    "configuration": {
      "title": "Liquibase",
      "properties": {
        "liquibase.liquibaseConfigurationFiles": {
          "type": "object",
          "markdownDescription": "Configures the stored Liquibase configurations.\n\nThe **Item** contains a unique name for your configuration. This name will be used in any dialogs.\n\nThe **Value** contains the path to your `liquibase.properties` file.",
          "default": {},
          "additionalProperties": {
            "type": "string"
          }
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test",
    "install:all": "npm install && cd webview-ui && npm install",
    "start:webview": "cd webview-ui && npm run start",
    "build:webview": "cd webview-ui && npm run build"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^6.17.0",
    "@typescript-eslint/parser": "^6.17.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3",
    "eslint-plugin-tsdoc": "^0.2.17"
  },
  "dependencies": {
    "properties-file": "^3.3.15"
  }
}
